.TH _PRINTF 3 "November 2025" "Holberton School" "Library Functions Manual"

.SH NAME
_printf \- custom implementation of the standard printf function

.SH SYNOPSIS
.B #include "main.h"
.PP
.B int _printf(const char *format, ...);

.SH DESCRIPTION
.B _printf()
is a custom implementation of the C standard library function
.B printf().
It produces output according to a
.I format
string and a variable number of arguments.

.PP
The function parses the format string and handles ordinary characters
as well as conversion specifiers beginning with the percent character (%).

.PP
The following conversion specifiers are handled:

.TP
.B %c
Prints a single character.

.TP
.B %s
Prints a null-terminated string.

.TP
.B %%
Prints a literal percent sign.

.TP
.B %d, %i
Prints a signed decimal integer.

.SH RETURN VALUE
The
.B _printf()
function returns the total number of characters printed,
excluding the null byte used to end output to strings.

.PP
If an error occurs (such as a NULL format string), the function returns
.B -1.

.SH EXAMPLES
.EX
_printf("Hello %s!\\n", "World");
Output: Hello World!

_printf("Number: %d\\n", 42);
Output: Number: 42
.EE

.SH AUTHORS
Written by:
.IP \[bu] 2
Your Name <your_email@example.com>
.IP \[bu]
Your Partner <partner_email@example.com>

Students at Holberton School.

.SH SEE ALSO
printf(3)
